# NetApp ONTAP MCP Server Configuration Example

# Copy this file to .env and update with your ONTAP cluster details

# Method 1: Single cluster (legacy - for backward compatibility)
ONTAP_CLUSTER_IP=192.168.1.100
ONTAP_USERNAME=admin
ONTAP_PASSWORD=your-password-here

# Method 2: Multiple clusters (recommended for multi-cluster environments)
# Set this environment variable to pre-register multiple clusters
ONTAP_CLUSTERS='[
  {
    "name": "cluster-1",
    "cluster_ip": "192.168.1.100",
    "username": "admin",
    "password": "your-password-here",
    "description": "Production ONTAP cluster"
  },
  {
    "name": "cluster-2", 
    "cluster_ip": "192.168.1.101",
    "username": "admin",
    "password": "your-password-here",
    "description": "Development ONTAP cluster"  
  }
]'

# Usage:
# 1. Copy this file to .env
# 2. Update with your actual cluster details
# 3. Source the environment: source .env
# 4. Start the MCP server: npm start

# SSL Configuration (optional)
ONTAP_VERIFY_SSL=false

# Default SVM (optional)
DEFAULT_SVM=svm1

# API Version (optional, defaults to latest)
ONTAP_API_VERSION=v1

# Common Volume Sizes
# Use these as reference for create_volume tool:
# Small: 10GB, 50GB, 100GB
# Medium: 500GB, 1TB, 2TB  
# Large: 5TB, 10TB, 20TB

# Example MCP Configuration for VS Code
# Add this to your VS Code settings or MCP configuration:
#{
#  "servers": {
#    "netapp-ontap-mcp": {
#      "type": "stdio",
#      "command": "node",
#      "args": ["/path/to/your/ontap-mcp/build/index.js"]
#    }
#  }
#}
